<ul>
	{{burger_name}}
	{{#unless devoured}}
	<button class="devourBurger" data-id="{{id}}" data-newdevour="{{devoured}}">
		devour
	</button>
	{{/unless}}
</ul>

<ul>
	{{#if devoured}}
	<button class="deleteBurger" data-id="{{id}}">
		delete
	</button>
	{{/if}}
</ul>